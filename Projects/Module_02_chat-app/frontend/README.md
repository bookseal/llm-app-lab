# frontend

React + Vite chat UI. Talks to the backend at `/api/chat` (proxied to `http://localhost:5000` in dev).

## Setup

```bash
cd frontend
npm install
npm run dev
# Running on http://localhost:5173
```

Make sure the backend is running on port 5000 in another terminal.
