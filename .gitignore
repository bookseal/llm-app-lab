# Secrets — never commit real API keys (.env.example is the safe template)
.env

# Python
.venv/
__pycache__/
*.pyc

# Node
node_modules/
frontend/dist/

# Generated at build time from git log (not committed)
docs/history.html

# RAG index artifacts — regenerated by `python indexer.py` (large binaries, don't commit)
*.pkl
index.meta.json

# OS
.DS_Store
